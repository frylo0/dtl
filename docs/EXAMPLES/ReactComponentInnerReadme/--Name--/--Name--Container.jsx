/*
 * Author:  Fedor Nikonov (fritylo)
 * Date:    --DateTime--
 * Company: frity corp.
 */

import { connect } from "react-redux";
import { --Name-- } from "./--Name--";

const state2props = state => ({
   
});

const dispatch2props = dispatch => ({
   
});

export const --Name--Container = connect(state2props, dispatch2props)(--Name--);
